# NextBeat Backend - Magenta Dependencies (OPTIONAL)
#
# ONLY use this if you want DrumsRNN / Music Transformer AI continuation.
# Requires Python <=3.11 (Magenta does not support 3.12+).
#
# Install order matters — install this INSTEAD of requirements.txt:
#   pip install -r requirements-magenta.txt
#
# On Apple Silicon:
#   pip install tensorflow-macos tensorflow-metal
#   pip install -r requirements-magenta.txt
#
# Why separate? Magenta pins old versions:
#   mido==1.2.6, pretty-midi==0.2.9, numpy<2, etc.
# These conflict with modern packages, so we can't mix them.

# Web framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0

# Configuration
pydantic>=2.5.3
pydantic-settings>=2.1.0
python-dotenv>=1.0.0

# Claude API
anthropic>=0.40.0

# Magenta (pins its own mido, pretty-midi, numpy versions)
magenta>=2.1.4
note-seq>=0.0.5
